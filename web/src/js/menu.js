document.addEventListener("DOMContentLoaded", (event) => {
    /*
    function fixMenu() {
        var menu_items = document.querySelectorAll(".menu > *, .menu > * > *")
        if (menu_items != null) {
            menu_items.forEach((item, index) => {
                if (item.style.display == "none" || item.style.display == undefined) {
                    item.style.display = "inline-block"
                    var isOverflowing = item.clientWidth < item.scrollWidth || item.clientHeight < item.scrollHeight
                    item.style.display = "none"
                } else {
                    var isOverflowing = item.clientWidth < item.scrollWidth || item.clientHeight < item.scrollHeight
                }
                console.log(isOverflowing)
                if (isOverflowing) {
                    console.log(item.style.fontSize)
                    item.style.fontSize = itemitem.style.fontSize
                }
            })
        }
    }

    fixMenu()

    window.addEventListener("resize", (event) => {
        fixMenu()
    })
    */
})